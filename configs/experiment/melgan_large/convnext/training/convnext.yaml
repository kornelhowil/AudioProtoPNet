# @package _global_
defaults:
  - override /datamodule: deepfake.yaml
  - override /module: multilabel.yaml
  - override /module/network: convnext.yaml
  - override /callbacks: default.yaml
  - override /trainer: single_gpu.yaml
  - override /datamodule/transforms: bird_default_multilabel.yaml
  - override /paths: default.yaml
  - override /hydra: default.yaml

task_name: "melgan_large_convnext"
tags: ["melgan_large", "DeepFake", "convnext"]
seed: 42
train: True
test: False

logger:
  wandb:
    tags: ${tags}
    group: "melgan_large_convnext"
    mode: online

trainer:
  min_epochs: 1
  max_epochs: 10

datamodule:
  dataset:
    hf_name: ljspeech_melgan_large # Default wavefake method
  transforms:
    preprocessing:
      mean: -13.369 # Mean of XCL
      std: 13.162 # Standard deviation of XCL