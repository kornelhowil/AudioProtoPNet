# @package _global_
defaults:
  - override /datamodule: deepfake.yaml
  - override /datamodule_push: deepfake.yaml
  - override /module: audioprotopnet_multilabel.yaml
  - override /module/network: ppnet_convnext.yaml
  - override /callbacks: default.yaml
  - override /trainer: single_gpu.yaml
  - override /datamodule/transforms: bird_default_multilabel.yaml
  - override /paths: default.yaml
  - override /hydra: default.yaml

task_name: "full_band_melgan_audioprotopnet"
tags: ["full_band_melgan", "DeepFake", "full_band_melgan_audioprotopnet"]
seed: 42
train: True
test: False
save_prototype_waveform_files: False
save_prototype_spectrogram_files: False

logger:
  wandb:
    tags: ${tags}
    group: "full_band_melgan_audioprotopnet"
    mode: online

module:
  num_prototypes: 10

callbacks:
  model_checkpoint:
    filename: ${module.network.model_name}_${module.training_phase}

datamodule:
  dataset:
    hf_name: ljspeech_full_band_melgan # Default wavefake method
  transforms:
    preprocessing:
      mean: -13.369 # Mean of XCL
      std: 13.162 # Standard deviation of XCL

datamodule_push:
  dataset:
    hf_name: ljspeech_full_band_melgan # Default wavefake method