defaults: 
  - override hydra_logging: colorlog
  - override job_logging: colorlog 

job:
  chdir: True
run:
  dir: ${paths.log_dir}/${task_name}/runs/${datamodule.dataset.dataset_name}/${module.network.model_name}
sweep: 
  dir: ./output/${task_name}/multiruns/${datamodule.dataset.dataset_name}/${module.network.model_name}
  subdir: ${hydra.job.num}